node_modules/
dist/
.DS_Store
*.log
.idea/
.env
.env.bedrock
.env.kimi

# dev-only 调试通道遥测（server/debug.mjs 落盘，仅本地定位问题用）
.dev-telemetry.jsonl

# intent A/B 影子数据（standalone 代理落盘）
.ab-intent.jsonl

# pdfjs CMap/字体资产：postinstall 自动从 node_modules 拷入，不入库（见 scripts/copy-pdfjs-assets.mjs）
public/cmaps/
public/standard_fonts/

# 协作方 InkSurface SDK（WS3）：独立项目·自带 git 仓库，不进我们这个仓库。接线产物走 src/integration/inksurface/。
ink-surface-sdk-main/
