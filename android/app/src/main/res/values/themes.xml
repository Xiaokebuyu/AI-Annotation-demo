<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 用平台主题避免引入 appcompat/material 依赖（套壳只需一个全屏 WebView）。 -->
    <style name="Theme.InkLoop" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
